export default class Limite {

	constructor(parameters) {
        this.id = parameters.id;
		this.limite = parameters.limite;
		this.monto = parameters.monto;
		this.categoria = parameters.categoria;
	}
}